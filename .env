# Database Configuration
DB_USER=chatapp
DB_PASSWORD=CHANGE_ME_IN_PRODUCTION
DB_NAME=chatapp

# JWT Configuration
# IMPORTANT: Change JWT_SECRET to a long random string in production
# Generate with: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_USE_LONG_RANDOM_STRING
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# Frontend Configuration
# For production with nginx proxy, leave empty (endpoints already include /api)
# For development with separate frontend/backend, use http://localhost:3000
VITE_API_URL=
